# Gemini API key — obtenela en https://aistudio.google.com/apikey
# Sin esta key no podés usar el backend Gemini API.
GEMINI_API_KEY=

# Master key para el proxy liteLLM (generá una segura, ej: openssl rand -hex 16)
LITELLM_MASTER_KEY=sk-local-dojo-agent
